# Blacktop Takeover

A responsive, server-rendered PHP prototype for a South African community basketball tournament platform. No frontend framework is required.

## Local setup

1. Start Apache and MySQL in XAMPP.
2. Import `database/blacktop_takeover.sql` in phpMyAdmin.
3. Visit `http://localhost/blacktop-takeover/`.

Database defaults target a standard local XAMPP install. Override them with `DB_HOST`, `DB_USER`, `DB_PASSWORD`, and `DB_NAME` environment variables when needed.
